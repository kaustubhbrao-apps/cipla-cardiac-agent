# ── Secrets ────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
gemini_key.txt
.vercel

# ── Licensed case data — MUST NEVER BE COMMITTED ───────────────────────────
# IQVIA/PharmaTrac data licensed to Cipla for Ascend Season 4. The source
# workbook lives outside this repo on purpose; only aggregated archetype-level
# results are published, generated by build/build_data.py.
*.xlsx
*.xls
*.csv
Case Study*.pdf

# ── Build noise ────────────────────────────────────────────────────────────
node_modules/
__pycache__/
*.pyc
.DS_Store

# generated by the Vercel python runtime
uv.lock
