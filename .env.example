# Copy to .env.local and fill in. Never commit the filled version.
GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=      # server-side only, never exposed to the browser
ASK_PASSWORD=              # shared secret gating /api/ask so the key can't be drained
